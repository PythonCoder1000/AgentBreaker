[project]
name = "agentbreaker"
version = "0.1.0"
description = "A workspace for probing and red-teaming AI agents."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.69",
    "python-dotenv>=1.0",
    "rich>=15.0.0",
]

[tool.uv]
package = false
